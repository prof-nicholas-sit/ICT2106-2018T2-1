using System;
using System.Collections.Generic;
using System.Linq;
using System.IO;
using System.Threading.Tasks;
using TypeSetter.Controllers;

namespace TypeSetter.DAL
{
	public class FileManagerGateway : iFileManager
	{
		public void openFile(string fileName)
		{
			//done by other group
			

			//GenericController genericController = new GenericController();
			//genericController.preparePreview(interpreterJob);
		}

		public void saveFile(InterpreterJob interpreterJob)
		{
			//done by the other group.
		}
	}
}
